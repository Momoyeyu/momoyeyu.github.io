---
import ArchivePanel from "@components/ArchivePanel.svelte";
import { ORDERED_CATEGORIES } from "@constants/categories.ts";
import I18nKey from "@i18n/i18nKey";
import { i18n } from "@i18n/translation";
import MainGridLayout from "@layouts/MainGridLayout.astro";
import { getSortedPostsList } from "../utils/content-utils";

const sortedPostsList = await getSortedPostsList();
const orderedCategories = [...ORDERED_CATEGORIES];
---

<MainGridLayout title={i18n(I18nKey.archive)}>
    <ArchivePanel sortedPosts={sortedPostsList} orderedCategories={orderedCategories} client:only="svelte"></ArchivePanel>
</MainGridLayout>

